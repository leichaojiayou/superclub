<view class="top">
  2017"西安正大学校"杯杨凌农科城马拉松热身赛开始报名啦！
</view>

<!-- 第一个 -->
<view class="item">
  <view class="text">
    成人票￥120.00
  </view>
</view>
<view>
  <scroll-view scroll-x scroll-y>
    <view class="body">
    <!-- 标题-->
      <view class="body_item">
        <block wx:for="{{title}}" for-index="index">
          <view wx:if="{{index%2==0}}" class="body_item_1" style="  background: rgb(255, 255, 255);">
            {{item}}
          </view>
          <view wx:else class="body_item_1" style="background: rgb(247, 255, 253);">
            {{item}}
          </view>
        </block>
      </view>
      <!-- 标题下的内容 -->
      <view class="body_item">
        <block wx:for="{{contents}}" for-index="index">
          <view wx:if="{{index%2==0}}" class="body_item_1" style="  background: rgb(255, 255, 255);">
            {{item}}
          </view>
          <view wx:else class="body_item_1" style="background: rgb(247, 255, 253);">
            {{item}}
          </view>
        </block>
      </view>
    </view>
  </scroll-view>
</view>




<!-- 第二个-->

<view class="item">
  <view class="text">
   儿童票￥60.00
  </view>
</view>
<view>
  <scroll-view scroll-x scroll-y>
    <view class="body">
    <!-- 标题-->
      <view class="body_item">
        <block wx:for="{{title}}" for-index="index">
          <view wx:if="{{index%2==0}}" class="body_item_1" style="  background: rgb(255, 255, 255);">
            {{item}}
          </view>
          <view wx:else class="body_item_1" style="background: rgb(247, 255, 253);">
            {{item}}
          </view>
        </block>
      </view>
      <!-- 标题下的内容 -->
      <view class="body_item">
        <block wx:for="{{contents}}" for-index="index">
          <view wx:if="{{index%2==0}}" class="body_item_1" style="  background: rgb(255, 255, 255);">
            {{item}}
          </view>
          <view wx:else class="body_item_1" style="background: rgb(247, 255, 253);">
            {{item}}
          </view>
        </block>
      </view>
    </view>
  </scroll-view>
</view>