<!--template/toasts/toasts.wxml-->

<!--template/common/common.wxml-->

<!--报名成功或报名审核弹框-->
<template name="apply">
  <view class="apply-succeed" style="{{applySucceed}}">
    <view class="apply-succeed-toast">
      <image wx:if="{{check==0}}" class="apply-succeed-image" src="../images/报名成功@2x.png"></image>
      <image wx:if="{{check==1}}" class="apply-succeed-image" src="../images/报名待审核@2x.png"></image>
      <view wx:if="{{check==0}}" class="apply-succeed-content">报名成功</view>
      <view wx:if="{{check==1}}" class="apply-succeed-content">你的报名信息已提交主办方审核</view>
      <button class="apply-succeed-btn" bindtap="hideToasts">确定</button>
    </view>
  </view>
</template>

<!--拍照上传弹框-->
<template name="check">
  <view class="check-succeed" style="{{applySucceed}}">
    <view class="check-succeed-toast">
      <image class="check-succeed-icon" mode="widthFix" src="../images/x@2x.png" bindtap="closeCheck"></image>
      <view class="check-succeed-title">示例</view>
      <view class="check-succeed-content">请上传合适的身份证自拍照</view>
      <image class="check-succeed-img" mode="widthFix" src="{{img}}"></image>
      <view style="position:relative">
        <view class="check-succeed-btn">上传</view>
        <view class="check-succeed-line"></view>
        <view class="check-succeed-btn">拍照</view>
      </view>
    </view>
  </view>
</template>

<!--俱乐部二维码弹框-->
<template name="clubCode">
  <view class="code-succeed" style="{{codeHehe}}">
    <view class="code-succeed-toast" style="background:url(../images/二维码背景@2x.png) no-repeat center center">
      <view class="code-message">
        <image mode="widthFix" class="code-message-logo" src="{{clubimg}}"></image>
        <image mode="widthFix" class="code-message-icon" src="{{clubicon}}"></image>
      </view>
      <view class="code-content-title">{{clubname}}</view>
      <view class="code-content-text">{{clubtext}}</view>
      <image mode="widthFix" class="code-message-img" src="{{clubcode}}"></image>
      <button class="code-content-btn">保存二维码</button>
    </view>
  </view>
  <view class="code-text" style="{{codeHehe}}">
    <view class="code-share">直接分享到俱乐部</view>
    <image mode="widthFix" class="code-close" src="https://cdn.51julebu.com/xiaochengxu/image/ewm-close@2x.png" bindtap="closeCode"></image>
  </view>
</template>

<!--活动二维码弹框-->
<template name="activity-Code">
  <view class="activity-code-succeed" style="{{activityCode}};">
    <view class="activity-code-toast" style="background:url(../images/二维码背景@2x.png) no-repeat center center">
      <image mode="widthFix" class="activity-code-img" src="{{activitycode}}"></image>
      <view class="activity-code-title">{{clubtitle}}</view>
      <button class="activity-code-btn">保存二维码</button>
    </view>
  </view>
  <view class="activity-code-text" style="{{activityCode}}">
    <view class="activity-code-share">直接分享到俱乐部</view>
    <image mode="widthFix" class="activity-code-close" src="https://cdn.51julebu.com/xiaochengxu/image/ewm-close@2x.png" bindtap="closeCode"></image>
  </view>
</template>

<!--活动验证码弹框-->
<template name="sendNum">
  <view class="modal" style="{{modalShowStyle}}">
    <view class="dialog">
      <view class="modol-text">
        已向你的手机{{phone}}发送4位验证码，请输入
      </view>
      <view class="modal-box">
        <input class="model-input" maxlength="4" type="number" focus="{{focus}}" bindinput="titleInput" value="{{diaryTitle}}" placeholder="输入验证码" placeholder-style="font-size:28rpx;letter-spacing:1rpx;" />
        <view wx:if="{{status==0}}" class="modal-time" style="color:#3dd1a5" bindtap="sendNum">发送验证码</view>
        <view wx:if="{{status==1}}" class="modal-time">重发{{time}}s</view>
        <view wx:if="{{sendstatus==1}}" class="modal-box-tip">注：下次报名将不再验证</view>
      </view>
      <view class="modal-button">
        <view class="modal-hehe">
          <button class="modal-btn modal-cancel" bindtap="touchCancel">取消</button>
          <button class="modal-btn modal-canadd" bindtap="touchAddNew">确定</button>
        </view>
      </view>
    </view>
  </view>
</template>

<!--手机号码弹框-->
<template name="sendPhone">
  <view class="phone" style="{{phoneShowStyle}}">
    <view class="phone-dialog">
      <view class="phone-text">
        请输入手机号码
      </view>
      <view class="phone-box">
        <input class="phone-input" maxlength="11" type="number" focus="{{getFocus}}" bindinput="titleInput" value="{{phoneNum}}" placeholder="输入" placeholder-style="font-size:28rpx;letter-spacing:1rpx;"></input>
      </view>
      <view class="phone-button">
        <view class="phone-hehe">
          <button class="phone-btn phone-cancel" bindtap="touchCancel">取消</button>
          <button class="phone-btn phone-canadd" bindtap="touchAddNew">确定</button>
        </view>
      </view>
    </view>
  </view>
</template>

<!--发送到邮箱弹框-->
<template name="sendEmail">
  <view class="email" style="{{emailShowStyle}}">
    <view class="email-dialog">
      <view class="email-text">
        发送到邮箱
      </view>
      <view class="email-box">
        <input class="email-input" focus="{{getFocus2}}" bindinput="titleInput" value="{{emailNum}}" placeholder="输入邮箱地址" placeholder-style="font-size:28rpx;letter-spacing:1rpx;"></input>
      </view>
      <view class="email-button">
        <view class="email-hehe">
          <button class="email-btn email-cancel" bindtap="touchCancel">取消</button>
          <button class="email-btn email-canadd" bindtap="touchAddNew">确定</button>
        </view>
      </view>
    </view>
  </view>
</template>
